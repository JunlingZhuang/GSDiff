{
  "name": "pixel-plan-ai",
  "version": "0.1.0",
  "private": true,
  "description": "AI code-generated semantic pixel floor plans with door placement.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "python backend/server.py",
    "test": "python -m unittest discover -s tests -v"
  },
  "devDependencies": {
    "typescript": "5.4.2"
  }
}
